# Telegram Bot Token (required)
# Get this from @BotFather on Telegram
TELEGRAM_BOT_TOKEN=your_bot_token_here

# Environment (development uses SQLite, production uses PostgreSQL)
NODE_ENV=development

# PostgreSQL connection string (required for production)
# Format: postgresql://user:password@host:port/database
# DATABASE_URL=postgresql://user:password@localhost:5432/something_sweet

# Disable SSL for local PostgreSQL (optional)
# DATABASE_SSL=false
